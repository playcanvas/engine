import { RenderPass } from "../../platform/graphics/render-pass.js";

/**
 * A render pass implementing rendering to a depth buffer only.
 *
 * @ignore
 */
class RenderPassDepth extends RenderPass {
}

export { RenderPassDepth };
