/**
 * @enum {number}
 * @name pc.gfx.ClearFlag
 * @description Constants for controlling the clearing of different buffer types.
 */
pc.gfx.ClearFlag = {
    /** Clear the color buffer. */
    COLOR: 1,
    /** Clear the depth buffer. */
    DEPTH: 2,
    /** Clear the stencil buffer. */
    STENCIL: 4
};